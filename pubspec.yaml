name: phone_number_text_input_formatter
description: A lightweight and efficient solution for formatting phone numbers within your Flutter applications.
version: 0.0.1
homepage:
repository: https://github.com/summerhammer/phone_number_text_input_formatter

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
